# 更新记录

## 0.5.0 — 2026-09-20

- 对照三个参考项目的关键源码路径，新增可追溯的源码分析与采纳记录。
- 已有 Reality 节点可在节点管理中切换携程／西瓜视频；保留端口和凭据，同步更新服务端目标与原始链接。
- 新增/替换分流规则时检测 DNS 解锁与代理出口的父子域名重叠；旧规则保留并显示提示。
- 修复旧版 ACME 证书续期后应用失败，后续检查可能误判来源而跳过重试的问题。

## 0.4.0 — 2026-09-20

- Reality 新节点提供携程 `www.ctrip.com` / 西瓜视频 `www.ixigua.com` 两个固定选择；已有节点保留原域名。
- 部署前检查 TLS 1.3、HTTP/2、证书链及域名匹配，失败时停止新增节点。
- 首页增加证书管理，按域名展示有效期、签发者、共用节点数及自动续期任务状态，可手动检查续期。
- 证书来源随证书版本一起保存；外部导入证书不会因遗留 ACME 目录而被自动续期覆盖。
- 新增 `dodo-sbox certs` / `dodo-sbox certs status` 快捷命令。

## 0.3.0 — 2026-09-20

- 自动放行活动 UFW / firewalld 中的节点 TCP / UDP 端口，变更与删除同步清理本脚本规则。
- 证书申请和续期期间临时放行验证端口，正常结束或失败退出后清理。
- UFW 使用独立应用规则，firewalld 分别记录新增的运行时和永久规则；保留已有权限。
- 增加 `dodo-sbox firewall`，为升级前已部署的节点补齐规则。
- 端口同步失败时回滚节点配置；卸载只清理本脚本管理的规则。

## 0.2.0 — 2026-09-20

- README 改为简明使用说明，安装入口置顶；新增一行安装命令和下载校验引导。
- 新增部署、节点管理、分流、日志、重启、更新和卸载快捷命令。
- 一键联合更新脚本和该发布版验证的 sing-box 核心，核心更新失败时恢复本次替换前的脚本。
- 可选每日自动更新，独立 systemd timer，默认关闭；开启、关闭和状态查询均可从菜单操作。
- 增加下载被篡改时拒绝执行、更新失败回滚、真实 systemd 定时任务与卸载检查。

## 0.1.0 — 2026-09-20

首个公开测试版本。

- Bash 管理器，固定版本 sing-box 核心，单服务管理多个独立节点。
- VLESS Reality、AnyTLS、Hysteria2；端口范围 10000–50000。
- 原始协议链接、本地二维码、节点启停、改名、端口和凭据管理。
- Let's Encrypt 自动签发、每日续期检查；独立 ACME 目录，无 Cloudflare API。
- 指定平台的解锁 DNS 或 SOCKS5 出口，其他流量保持本机直连。
- 原子切换配置、启动失败回滚、脚本与核心分别更新、独立卸载。
- 主菜单展示服务、节点数量、分流和续期状态；节点支持按序号选择。
- GitHub Actions 检查 Shell、可复现发布包和真实 TLS 代理请求。

实测和未验证的环境见 [TEST_REPORT.md](TEST_REPORT.md)。
