# 重写版实测记录

日期：2026-09-20；初始实机测试版本：0.1.0-dev；公开发布版本：0.1.0；核心：sing-box 1.14.1。下方实机测试记录来自发布前的开发版本，发布改动另行列出。

## 已通过

## 0.2.0 更新验证

- 本地 10 项原有检查和 5 项更新检查通过；包含真实核心配置校验、模拟启动失败恢复、停止状态保持、联合更新失败恢复管理脚本。
- GitHub Ubuntu 24.04 CI 实测下载引导：正确 SHA256 可执行，错误校验值和非法清单拒绝执行。
- GitHub 独立 systemd 环境实测安装、开启/关闭自动更新 timer、状态显示及卸载清理。测试用临时单元条件阻止定时任务实际访问尚未发布的版本；此项验证调度配置和生命周期，未等待次日触发。
- 协议更新采用 sing-box 核心整体更新；自动更新只使用当前项目发布版验证过的核心，不自动追随上游未验证版本。
- 此轮没有修改用户测试服务器的现有程序，也没有替用户开启每日更新。

## 初始版本验证

- Bash 语法检查、发布包 ShellCheck warning 级检查。
- 本地 macOS arm64：端口边界、非法域名、重复节点/端口、错误协议、缺少分流配置的拒绝；IPv6 与中文/特殊字符 URI 编码。
- macOS：三个协议生成的配置通过真实核心校验；AnyTLS、Hysteria2 完成真实代理请求并校验测试证书。
- Ubuntu 24.04 amd64：独立 systemd 服务同时运行三个协议，真实 sing-box 客户端逐个转发 HTTPS 请求。
- Mac → 测试服务器 → 外网：Reality、AnyTLS、Hysteria2 均返回测试服务器出口 IP。AnyTLS/Hy2 使用真实 Let's Encrypt 证书，未关闭节点证书校验。
- 实际签发 Let's Encrypt 证书；独立每日续期 timer；手动触发续期检查成功；额外进行一次真实续期，确认序列号变化且新证书生效。
- 证书申请使用空闲 443 的 TLS-ALPN，没有停止或重载原 Nginx，结束后 443 释放。
- 非法输入不改变当前配置；制造真实端口冲突后，新服务启动失败并自动回滚。
- 节点启停、改名、修改端口、凭据重置、删除；菜单取消不改状态；从菜单部署并复用证书；原始 AnyTLS 链接及本地二维码生成。
- 错误 SHA256 更新被拒绝且原脚本不变；正确安装包更新成功；重复安装幂等；当前核心版本不重复升级；尚无新版 Release 安装包时保留当前脚本。
- 受控分流实验：选中域名由指定 DNS 解析并连接其返回的地址，其他域名保持本机解析；选中域名进入认证 SOCKS5 出口，其他请求直连；出口失败不会回落，也不影响普通请求。
- 确认并记录 DNS-only 的限制：客户端发送已解析 IP 时，SNI 嗅探不触发目的地址重写。未将此场景误报为解锁成功。

## 0.1.0 发布复核

- macOS 本地 10 项检查通过；GitHub Actions 在 Ubuntu 24.04 上通过 Bash 语法、ShellCheck、可复现构建和真实 TLS 代理请求检查。
- 从公开 GitHub Release 重新下载发布包，macOS 与测试服务器上的 SHA256 校验均通过。
- 在服务器上实际打开主菜单、进入 AnyTLS 部署步骤并取消；菜单没有部署节点或修改现有服务。[终端输出节选](docs/menu-preview.txt)。
- 在服务器私有隔离目录中调用真实在线更新功能：下载新仓库 Release、验证 SHA256、保留旧脚本，第二次调用提示已是最新发布版本。此项未切换运行中的服务。
- 原 sing-box、Nginx、哪吒和 SSH 的 PID、启动时间保持不变，原配置 SHA256 全部通过。

## 测试中发现并修复

- macOS jq 与 Ubuntu jq 的对象表达式解析差异：补充括号后，Linux 配置生成及三协议运行通过。
- OpenSSL `x509 -checkhost` 的退出码不足以判定域名匹配：改用带 `-verify_hostname` 的证书验证，增加正确域名/错误域名回归检查；实机确认错误域名证书被拒绝且当前证书序列号不变。
- ACME 证书导出使用持久目录，避免续期引用已经删除的临时输出路径；真实续期与新证书激活通过。

## 原服务保护

测试前保存原配置 tar 备份、配置 SHA256、服务 PID/启动时间、监听端口、IPv4/IPv6 防火墙、BBR 设置。原 sing-box、Nginx、哪吒继续运行。测试使用独立目录、服务、用户与节点端口；外部连通测试轮流使用原本已放行且空闲的端口，没有添加或更改防火墙规则。

最终卸载测试通过：新增服务、续期 timer、独立用户、运行目录和快捷命令均已移除。原配置 SHA256、原服务 PID/启动时间、IPv4/IPv6 防火墙规则、系统 DNS、BBR 全部一致；原监听地址、端口和服务 PID 一致。systemd daemon-reload 后 PID 1 持有的 SSH socket 文件描述符编号发生变化，SSH 自身 PID、监听端口和配置均未变化。

原配置及测试证书/节点状态备份保存在测试服务器的 root 私有目录。备份、服务器连接信息和真实节点凭据均不进入公开仓库。

## 验证边界

- 实测 Linux 为 Ubuntu 24.04 amd64；Debian、其他 Ubuntu 版本、Linux arm64 未逐一真机验证。
- 实测真实签发路径为 TLS-ALPN；空闲 80 的独立 HTTP 签发、现有网站目录签发尚未实际向 CA 验证。
- 未对现有主机修改 BBR 或重启整机。BBR 写入/恢复及断电级恢复仍需隔离环境验证。
- 未提供真实流媒体解锁 DNS 或远端地区出口，尚未验证 Netflix 等平台的实际地区与播放；受控实验仅证明 DNS/代理分流行为。
- 客户端连通实测使用 sing-box CLI；Surge、Mihomo、小火箭的原始链接导入与 GUI 操作未逐一验证。
- 首次公开版本仍属于测试阶段，不能据此称为所有环境稳定版。
